## Проблема

Бейдж атрибуции **уже добавлен** на обе карты (`MapView.tsx` строка 1952, `TbilisiMap.tsx` строка 782), но размещён в правом нижнем углу (`bottom-1.5 right-14`) поверх стандартной полосы атрибуции MapLibre/OSM. Текст 10.5px на белом фоне OSM-плашки сливается, а на мобильных вообще перекрывается. Поэтому его не видно.

Плюс на картах нет кнопки «домой» — выйти на лендинг можно только через адресную строку.

## Что сделаю

### 1. Сделать бейдж атрибуции явно заметным

В `src/components/AuthorCredit.tsx` → `MapAuthorBadge`:
- Перенести в **левый нижний угол поверх плашки масштаба** (или верхний левый под кнопкой Home — см. ниже), чтобы не конкурировать с OSM-атрибуцией.
- Увеличить размер шрифта с 10.5px до 12px, плотнее тень, более контрастный фон (`bg-card` без прозрачности), `z-[10]`.
- На мобильных оставить компактный вариант `© 2025 В. Штыбин · datatells.info`, на десктопе — полное имя.
- Иконка внешней ссылки (`ExternalLink`) рядом с текстом — чтобы было понятно, что это кликабельно.

### 2. Добавить кнопку «На главную» на карты

Новый компонент `HomeButton` в `src/components/AuthorCredit.tsx` (или отдельный `MapHomeButton.tsx`):
- Маленькая круглая/пилюлевидная кнопка в **левом верхнем углу карты** (там сейчас пусто на обеих картах).
- Иконка `Home` из lucide + текстовый лейбл по языку: «На главную» / «Home» / «მთავარზე».
- TanStack Router `<Link to="/" search={{ lang }}>` — сохраняет текущий язык.
- Стиль: `bg-card/95 border border-border shadow-md backdrop-blur`, `z-[10]`.

Подключу в:
- `src/components/map/MapView.tsx` (общая карта Грузии)
- `src/components/map/TbilisiMap.tsx` (карта Тбилиси)

НЕ добавляю в `/embed` — embed-режим встраивается на сторонние сайты, кнопка «на главную» там не нужна.

### 3. Проверка

После правок сделаю скриншоты `/map?lang=ru` и `/tbilisi?lang=ru`, чтобы убедиться что и бейдж, и кнопка хорошо видны и не перекрывают элементы карты (легенда слева снизу, панель статистики справа сверху).

## Что НЕ трогаю

- Логику карт, данные, RLS, маршрутизацию.
- Лендинг, гайд, README — там бейдж/атрибуция уже на месте.
- `/embed` маршрут.
