## Цель

Сделать авторство **Виталия Штыбина** и копирайт явно видимыми во всех точках входа проекта, добавить на главной отсылку на datatells.info с приглашением посмотреть другие материалы автора, и зафиксировать те же сведения в гайде, документации карты и README в репозитории.

## Что меняем

### 1. Главная страница (`src/routes/index.tsx`)
- Добавить заметный блок «Автор проекта» в самом верху или сразу под hero: имя **Виталий Штыбин**, краткая подпись («независимый исследователь, автор datatells.info»), CTA-кнопка **«Другие проекты и статьи автора → datatells.info»** (внешняя ссылка `https://datatells.info`, `target="_blank" rel="noopener"`).
- В подвале страницы — копирайт-строка: `© 2025 Виталий Штыбин · Все права защищены · datatells.info` (на трёх языках).
- Все тексты — через `STRINGS[lang].landing` (новые ключи `author*`, `copyright`, `otherProjectsCta`) в `src/lib/i18n.ts` для ru / en / ka.

### 2. Карты (`src/components/map/MapView.tsx`, `TbilisiMap.tsx`, `src/routes/embed.tsx`)
- Поверх карты (как нижний overlay рядом с атрибуцией MapLibre/OSM) — компактная подпись `© Виталий Штыбин · datatells.info`, кликабельная, открывает datatells.info в новой вкладке.
- Показывать одинаково в `/map`, `/tbilisi` и `/embed` (в embed-режиме особенно важно — карта может быть встроена на чужом сайте).

### 3. Гайд (`src/routes/guide.tsx` и `public/docs/map-guide-{ru,en,ka}.md`)
- В начало каждого `map-guide-*.md` добавить блок «Автор / Author / ავტორი»: имя, ссылка на datatells.info, формулировка о защите авторских прав.
- В конец — расширенный copyright: «© 2025 Виталий Штыбин. Карты и сопроводительные материалы защищены авторским правом. При перепечатке и цитировании — указывать автора и ссылку на metrics.datatells.info».
- В `guide.tsx` (если рендерит свой шапочный блок) — тот же author-блок, чтобы видеть его без скачивания md.

### 4. Метаданные
- В `head()` главной и `/map` добавить `<meta name="author" content="Виталий Штыбин">` и JSON-LD `CreativeWork` с `author` и `copyrightHolder`.
- `public/llms.txt` — добавить строку «Author: Виталий Штыбин (datatells.info). Content is copyrighted».

### 5. README.md (GitHub)
- Под заголовком добавить бейдж/строку **«Автор: Виталий Штыбин — [datatells.info](https://datatells.info)»**.
- Раздел «Лицензия и благодарности» переписать: код — под MIT, **карты, данные и сопроводительные тексты — © 2025 Виталий Штыбин, все права защищены**; при использовании — указывать автора и ссылаться на metrics.datatells.info.
- Добавить отдельный короткий блок «Об авторе» со ссылкой на datatells.info и приглашением читать другие статьи.

### 6. Файл `LICENSE` / `COPYRIGHT`
- Создать `COPYRIGHT.md` в корне с явной формулировкой по-русски и по-английски: что именно защищено (картографические данные, сборки GeoJSON, тексты гайда, дизайн), как ссылаться, контакты автора.
- В существующем `LICENSE` (если MIT) оставить MIT для кода, в шапке указать `Copyright (c) 2025 Vitaly Shtybin`.

## Технические детали

- Все строки — через `STRINGS` (ru / en / ka), без хардкода в JSX, по правилам проекта.
- Внешние ссылки — `target="_blank" rel="noopener noreferrer"`.
- Overlay на карте — позиционировать так, чтобы не перекрывал существующие контролы (нижний левый угол, рядом с атрибуцией).
- Никакой бизнес-логики не меняем — только презентационный слой + статические md/README.

## Уточняющие вопросы (опционально)

- Год копирайта — `2025` или диапазон `2024–2026`?
- Email/контакт автора публиковать в COPYRIGHT.md, или достаточно ссылки на datatells.info?
- Формулировка прав: «Все права защищены» (строгая) или «CC BY 4.0 — с указанием автора» (разрешает переиспользование при атрибуции)? Второе — стандарт для открытых исторических проектов и совместимо с целью «видимое авторство».